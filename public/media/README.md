## Media folder

Put images you want to use on the site in this folder.

### Home hero (iPhone visual)

1. Add your image file here as: `home-iphone.png` (or `.jpg/.webp`)
2. Update `src/app/page.tsx`:

- Set `heroImageSrc="/media/home-iphone.png"` on the `PhoneMock` component.


